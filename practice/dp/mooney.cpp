// usaco time is mooney
#include <bits/stdc++.h>
using namespace std;

void solve() {
  int a;
  cin >> a;
}

int main() {
  // Optimize I/O operations
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, m, c;
  cin >> n >> m >> c;
  while (n--) {
    solve();
    cout << "\n";
  }

  return 0;
}
