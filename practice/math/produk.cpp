// TROC #33 > D
#include <iostream>
#include <vector>

#define ll long long

using namespace std;

int main() {
  int n;
  vector<ll> nums;
  for (int i = 0; i < n; i++) {
    int temp;
    cin >> temp;
    nums.push_back(temp);
  }
}
